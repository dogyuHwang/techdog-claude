---
name: tdc-learn
description: "TechDog Claude - 프로젝트를 자동 분석해 project-memory.md를 생성. patterns 서브커맨드로 세션 패턴 추출도 가능."
user-invocable: true
argument-hint: "[patterns [extract|list|apply <name>|clean]]"
---

**입력:** $ARGUMENTS

# /tdc-learn — Project Learning

기존 프로젝트의 기술 스택·구조·컨벤션을 자동 분석해 `.tdc/project-memory.md`에 저장합니다.
**이후 모든 tdc 세션이 이 지식을 활용하여 더 나은 결과를 냅니다.**

## 기본 동작 (`/tdc-learn`)

### 1단계: 기존 상태 확인

1. `.tdc/project-memory.md`가 이미 존재하면 → "이미 학습되어 있습니다. 재분석하시겠습니까? (y/N)" 확인
2. 없으면 → 바로 분석 시작

### 2단계: 프로젝트 분석

Explore Agent로 다음을 자동 스캔한다:

**기술 스택 감지** (파일 존재 여부로 판단):
- `package.json` → Node.js/TypeScript (dependencies에서 프레임워크 감지)
- `requirements.txt` / `pyproject.toml` / `setup.py` → Python
- `go.mod` → Go
- `Cargo.toml` → Rust
- `build.gradle` / `pom.xml` → Java/Kotlin
- `pubspec.yaml` → Flutter/Dart
- `next.config.*` → Next.js
- `manage.py` + `settings.py` → Django
- `angular.json` → Angular
- `vite.config.*` → Vite

**코딩 컨벤션 감지**:
- `.eslintrc*` / `.prettierrc*` → 린터/포매터 규칙
- `.editorconfig` → 탭/스페이스, 줄바꿈
- `tsconfig.json` → strict 여부 등
- 기존 코드 샘플링 → 네이밍 패턴 (camelCase/snake_case), import 스타일 (ESM/CJS)

**프로젝트 구조 감지**:
- 최상위 디렉토리 레이아웃
- 엔트리 포인트 (`main.*`, `index.*`, `app.*`)
- 테스트 디렉토리 + 프레임워크
- CI/CD (`.github/workflows/`, `Jenkinsfile`, `.gitlab-ci.yml`)

**빌드/실행 명령 감지**:
- `package.json` scripts 섹션
- `Makefile` 타겟
- `docker-compose.yml` 서비스
- `.env.example`

### 3단계: project-memory.md 생성

```markdown
# Project Memory — Auto-generated by /tdc-learn
# Last updated: {날짜}
# Re-run: /tdc-learn

## Tech Stack
- Language: {언어} ({버전})
- Framework: {프레임워크}
- Styling: {스타일링}
- DB: {DB + ORM}
- Test: {테스트 프레임워크}

## Conventions
- Import style: {ESM/CJS}
- Naming: {camelCase/snake_case}
- Formatter: {포매터 + 핵심 설정}
- Linter: {린터 + 핵심 규칙}

## Project Structure
- {주요 디렉토리 설명}

## Build & Test
- Build: `{명령}`
- Test: `{명령}`
- Lint: `{명령}`
- Dev: `{명령}`

## Business Rules
(비즈니스 규칙은 코드로 추론 불가 — 수동 추가)
```

### 4단계: 결과 보고

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  [TDC] PROJECT LEARNING COMPLETE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Tech Stack: TypeScript + Next.js 14 + Prisma
  Test: Jest + React Testing Library
  Structure: App Router (src/app/)

  Saved to: .tdc/project-memory.md

  이후 /tdc 실행 시 자동으로 이 정보를 활용합니다.
  비즈니스 규칙은 project-memory.md에 수동으로 추가하세요.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 서브커맨드: `patterns` (세션 패턴 학습)

현재 세션에서 반복 사용된 문제 해결 패턴을 추출해 재사용 가능한 스킬로 저장합니다.

### `/tdc-learn patterns extract`

**추출 대상:**
- 반복 디버깅 패턴 (에러 → 진단 → 해결)
- 특정 스택에서의 관용구 (예: "React hydration 에러 → useEffect 래핑")
- 프로젝트 특화 규칙 (예: "이 프로젝트에서는 항상 ESM import")
- 반복 코드 패턴 (예: "API 엔드포인트 추가 시 validation middleware")

**품질 게이트** (모두 충족해야 저장):
- 재사용 가능성 — 미래에 다시 쓰일 가능성이 높은가?
- 구체성 — 충분히 구체적인가? (너무 일반적이면 거부)
- 검증됨 — 이 세션에서 실제로 문제를 해결했는가?

**저장 경로**: `.tdc/learned-skills/<name>.md`

```markdown
---
name: <skill-name>
triggers: [keyword1, keyword2, ...]
category: debug|pattern|config|workflow
learned_from: <session_id>
confidence: high|medium
---

## Problem
<어떤 상황에서 이 패턴이 필요한가>

## Solution
<구체적 해결 단계>

## Example
<실제 적용 예시>
```

추출 후 사용자에게 내용을 보여주고 저장 여부를 확인한다.

### `/tdc-learn patterns list`

`.tdc/learned-skills/*.md` 테이블 출력: Name | Category | Triggers | Confidence | Date

### `/tdc-learn patterns apply <name>`

특정 학습 스킬을 현재 세션에 수동 적용한다. 매칭된 스킬 내용을 작업 컨텍스트에 주입.

### `/tdc-learn patterns clean`

confidence: low 이거나 30일 이상 미사용된 스킬을 정리.

### Auto-Injection (자동 주입)

Master Agent가 파이프라인 시작 시 `.tdc/learned-skills/`를 스캔하여:
1. 현재 태스크 키워드와 각 스킬의 `triggers`를 매칭
2. 매칭된 스킬 내용을 해당 에이전트에 컨텍스트로 전달
3. 자동 주입된 스킬은 `[SKILL-INJECTED] <name>` 태그로 로그 기록

**매칭 규칙:**
- confidence: high만 자동 주입 (medium은 `patterns apply`로 수동)
- 한 번에 최대 3개 주입 (토큰 절약)

## 토큰 최적화

- Explore Agent는 파일 존재 여부 위주 빠른 스캔 (내용은 최소)
- 설정 파일은 핵심 필드만 추출
- 패턴 스킬 파일은 200줄 미만 유지
- 자동 주입 시 Problem + Solution만 전달 (Example 생략)
